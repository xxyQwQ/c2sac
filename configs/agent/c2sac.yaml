name: c2sac

parameter:
  num_layers: 3
  hidden_dims: 1024
  actor_lr: 1e-4
  critic_lr: 3e-4
  discount_factor: 0.99
  update_rate: 0.01
  num_samples: 10
  penalty_weight: 10.0

strategy:
  share_interval: 100
  rank_percentage: 0.05
